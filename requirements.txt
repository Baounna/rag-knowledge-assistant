# --- slice 1: ingestion -------------------------------------------------
pypdf==6.16.2              # PDF text extraction

# --- slice 2: indexing --------------------------------------------------
psycopg[binary]>=3.2       # Postgres driver
httpx>=0.27                # hosted embedding providers
fastembed>=0.4             # local ONNX embeddings (default; no API key)

# --- slice 4: generation ------------------------------------------------
# anthropic>=0.69          # Claude API

# --- dev ----------------------------------------------------------------
pytest>=8.0
